# Quick Start
1) Create folders and place images:
   data/normal/*.png or .jpg
   data/disease/*.png or .jpg
2) Create a fresh venv and install:
   pip install -r requirements.txt
3) Run:
   python main.py --epochs 8 --batch 32 --img 128
4) Check outputs in artifacts/ (models, plots, results).
